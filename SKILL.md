---
name: ai-card-alchemy
description: AI 卡片炼金术 —— 把最近半年 AI 世界里的小卡片（人物故事 / 技术进展 / 名人语录）提取出来，套用 12 种图像处理风格重画成一张可以发的图。当用户说「提取这张 AI 卡片」「给这段 AI 内容配张图」「用禅意/水彩/旧报纸重画这张卡片」「从卡片库里挑一张」时使用。
agent_created: true
---

# AI 卡片炼金术 · AI Card Alchemy

> 丸子的无限进步

> 🌐 在线预览（直接看图）：https://hhpp55782-beep.github.io/ai-card-alchemy/

一句话：**挑一张卡片 → 挑一种风格 → 复制一条能直接用的处理指令。**

收录范围是 2026 年 3 月—9 月 AI 世界里值得留下来的那一瞬：人的故事、技术的进展、谁说过的话。

---

## 什么时候用我

- 「把这段 AI 新闻做成一张卡片」
- 「这句话我想配张图发出去，用禅意一点的方式」
- 「最近半年 AI 有什么有意思的小故事？给我挑几张」
- 「这张截图/卡片帮我提取一下内容，重画成水彩 / 旧报纸 / 铅笔素描」
- 「帮我也做一个这样的风格库工作台」

---

## 目录

```
ai-card-alchemy/
├── index.html                 单文件工作台（双击就能开，数据已内联）
├── data/alchemy.json           数据层：24 张卡片 + 12 种风格
├── assets/                     4 张风格样例图
├── scripts/build.py            把 JSON 注入 index.html
├── scripts/studio_server.py    本地扩充服务（8796 端口，可增卡片/风格/导出）
└── 打开卡片风格库.bat           Windows 双击启动
```

---

## 标准流程（三步）

### 第一步 · 挑卡片

打开 `index.html`，在「卡片库」里按类型筛选：

| 类型 | 有多少 | 底色 |
|---|---|---|
| 故事 | 8 张 | 暖橘 |
| 进展 | 8 张 | 青灰 |
| 语录 | 8 张 | 紫灰 |

也可以直接读 `data/alchemy.json` 的 `cards` 字段筛选。每张卡片的字段：
`type / title / core / body / source / date / tags / fit`

**挑卡片的硬规则**：卡片必须有可核查来源。要新增时，用联网搜索拿到原始出处再写进 `body`，不凭印象编。

### 第二步 · 挑风格

12 种风格分三类（完整提示词在 `data/alchemy.json` 的 `styles[].prompt`）：

**抽象提炼**（做减法）
- `zen-line` 禅意线稿 — 只留一条主轮廓 + 三处细节，其余空白
- `geo-block` 几何色块 — 5—8 个扁平色块，留白 40%+
- `one-ink` 一点墨 — 宣纸水墨，浓淡两档，右下朱红小印
- `paper-cut` 剪纸层叠 — 3—5 层同色系纸片叠压投影

**叙事重组**（换个讲法）
- `magazine` 杂志内页 — 大标题 + 导语 + 三栏正文 + 主图
- `old-press` 旧报纸 — 米黄纸、粗网点、砖红点缀、通栏大标题
- `journal` 手账拼贴 — 拍立得边框、胶带、手写批注
- `storyboard` 电影分镜 — 4—6 格，铅笔淡彩，下方一行场景说明

**材质转译**（换一种质感）
- `watercolor` 水彩淡彩 — 纸纹可见，边缘晕染，温柔明亮
- `pencil-sketch` 铅笔素描 — 排线铺背景，适合设备与实验
- `silver-gelatin` 银盐旧照 — 褪色、颗粒、暖调、四角压暗
- `blueprint` 工程蓝图 — 靛蓝底白线，尺寸线与图框

**快速匹配**：故事 → 水彩 / 手账 / 分镜 / 银盐；进展 → 旧报纸 / 蓝图 / 几何色块 / 素描；语录 → 禅意线稿 / 一点墨 / 几何色块。

### 第三步 · 生成指令

每条指令固定四段，**不要省略第四段的约束**：

```
一、目标风格：<名称>（<分类>）
二、原卡片内容：标题 / 一句话 / 正文 / 来源与时间
三、处理要求：<该风格的 prompt>
四、固定约束：
  1. 竖版 3:4，留可排文字的空白区
  2. 保留信息点与语气，不新增事实
  3. 右下角加「丸子的无限进步」手写体小字水印，画面内不出现其他多余文字
  4. 人物故事类不要生成可识别的真实人物肖像，用背影 / 手部 / 剪影 / 物件代替
```

第 4 条是底线：蔡磊、王宁这类真实人物故事，用物件和剪影表达，不要 AI 生成人脸。

---

## 常用操作

**打开工作台**：双击 `index.html`（离线可用）；或 `python scripts/studio_server.py` 后访问 `http://127.0.0.1:8796`。

**加一张卡片**（三种方式任选）
1. 起服务后 POST：
   ```bash
   curl -X POST http://127.0.0.1:8796/api/card -H "Content-Type: application/json" \
        -d '{"type":"语录","title":"标题","core":"一句话","body":"正文","source":"来源","date":"2026-09","tags":["a"],"fit":["zen-line"]}'
   ```
2. 直接编辑 `data/alchemy.json`，然后 `python scripts/build.py` 重建页面。
3. 让 AI 帮你写：点页面底部「复制给 AI 的添加请求」，把返回的模板交给 AI 填。

**加一种风格**：POST `/api/style`，字段 `name / category / prompt / keep / fit / source`。

**导出**：页面底部「导出全部卡片（Markdown）」，或 `GET /api/export`。

---

## 收录与版权

- 所有卡片正文均注明来源与时间，引用以原文为准。
- 风格为原创工作流设计与编写（AI 辅助），不主张发明传统艺术技法。
- 别人的 Skill 若作者声明不可转载，只保留名称与原链接，不复制提示词。
- 输出成果统一带「丸子的无限进步」水印。

---

## 想再做一个同款？

照这个骨架就行，只有三件事：

1. **数据层**：一个 JSON，两种对象 —— `cards`（内容）和 `styles`（处理方法，核心是 prompt 字段）。
2. **静态页**：单文件 HTML，数据用 `<script type="application/json">` 内联（这样 file:// 双击也能跑，不用 fetch）。筛选 + 搜索 + 弹窗 + 一键复制，几百行 JS 够用。
3. **本地扩充**：一个 Python 标准库写的 `http.server`，加个 POST 接口写回 JSON，再调 `build.py` 重新注入页面。

参考实现：`https://github.com/reneexiaoxiao/photo-alchemy-studio`

---

> 🌿 **丸子的无限进步** · 本技能出自「丸子的无限进步」技能库 — 小红书 / 视频号同名账号
